# PostgreSQL connection used by Prisma CLI and server-only database code.
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DATABASE?schema=public

# Server-only credential used when the Ticketmaster provider is invoked.
TICKETMASTER_API_KEY=
